# Laravel Documentable

Package skill placeholder — content added as later phases land (attaching `Documentable` to a
model, `allows_multiple`/`requires_versioning` composition, `etag_strategy` choice, DocumentType
catalog code-vs-DB choice, reaper scheduling). See `docs/implementations/phase-7-release-and-docs.md`.
